import React from 'react';
import Header from '../Layout/Header';

const file = () => {
    return (
        <div>
            <Header />
            파일관리페이지
        </div>
    );
};

export default file;